#!/usr/bin/env node

const commander = require('commander');

commander.version('1.0.0')
         .usage('<command> 「项目名称」')
         .command('cm', '「CaMnter」')
         .command('save', '「Save you from anything」')
         .command('init', '「Init test product」')
         .parse(process.argv)